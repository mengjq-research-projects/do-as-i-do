diff --git a/hawor/configs/__init__.py b/hawor/configs/__init__.py
index 569bf16..2c9dda5 100644
--- a/hawor/configs/__init__.py
+++ b/hawor/configs/__init__.py
@@ -116,5 +116,9 @@ def get_config(config_file: str, merge: bool = True, update_cachedir: bool = Fal
       cfg.MANO.MODEL_PATH = update_path(cfg.MANO.MODEL_PATH)
       cfg.MANO.MEAN_PARAMS = update_path(cfg.MANO.MEAN_PARAMS)
 
+    mano_right = os.environ.get("MANO_RIGHT")
+    if mano_right:
+      cfg.MANO.MODEL_PATH = os.path.dirname(os.path.abspath(mano_right))
+
     cfg.freeze()
     return cfg
diff --git a/hawor/utils/process.py b/hawor/utils/process.py
index 89bfc33..7c5f15c 100644
--- a/hawor/utils/process.py
+++ b/hawor/utils/process.py
@@ -4,6 +4,31 @@ from hawor.utils.geometry import aa_to_rotmat
 import numpy as np
 import sys
 import os
+from pathlib import Path
+
+
+_HAWOR_ROOT = Path(__file__).resolve().parents[2]
+
+
+def _mano_config(left=False):
+    if left:
+        data_dir = _HAWOR_ROOT / "_DATA/data_left"
+        model_file = os.environ.get("MANO_LEFT")
+        model_dir = Path(model_file).parent if model_file else data_dir / "mano_left"
+    else:
+        data_dir = _HAWOR_ROOT / "_DATA/data"
+        model_file = os.environ.get("MANO_RIGHT")
+        model_dir = Path(model_file).parent if model_file else data_dir / "mano"
+    config = {
+        'DATA_DIR': str(data_dir),
+        'MODEL_PATH': str(model_dir),
+        'GENDER': 'neutral',
+        'NUM_HAND_JOINTS': 15,
+        'CREATE_BODY_POSE': False,
+    }
+    if left:
+        config['is_rhand'] = False
+    return config
 
 def block_print():
     sys.stdout = open(os.devnull, 'w')
@@ -13,13 +38,7 @@ def enable_print():
 
 def get_mano_faces():
     block_print()
-    MANO_cfg = {
-        'DATA_DIR': '_DATA/data/',
-        'MODEL_PATH': '_DATA/data/mano',
-        'GENDER': 'neutral',
-        'NUM_HAND_JOINTS': 15,
-        'CREATE_BODY_POSE': False
-    }
+    MANO_cfg = _mano_config()
     mano_cfg = {k.lower(): v for k,v in MANO_cfg.items()}
     mano = MANO(**mano_cfg)
     enable_print()
@@ -37,13 +56,7 @@ def run_mano(trans, root_orient, hand_pose, is_right=None, betas=None, use_cuda=
     betas : (optional) B x D
     """
     block_print()
-    MANO_cfg = {
-        'DATA_DIR': '_DATA/data/',
-        'MODEL_PATH': '_DATA/data/mano',
-        'GENDER': 'neutral',
-        'NUM_HAND_JOINTS': 15,
-        'CREATE_BODY_POSE': False
-    }
+    MANO_cfg = _mano_config()
     mano_cfg = {k.lower(): v for k,v in MANO_cfg.items()}
     mano = MANO(**mano_cfg)
     if use_cuda:
@@ -115,14 +128,7 @@ def run_mano_left(trans, root_orient, hand_pose, is_right=None, betas=None, use_
     betas : (optional) B x D
     """
     block_print()
-    MANO_cfg = {
-        'DATA_DIR': '_DATA/data_left/',
-        'MODEL_PATH': '_DATA/data_left/mano_left',
-        'GENDER': 'neutral',
-        'NUM_HAND_JOINTS': 15,
-        'CREATE_BODY_POSE': False,
-        'is_rhand': False
-    }
+    MANO_cfg = _mano_config(left=True)
     mano_cfg = {k.lower(): v for k,v in MANO_cfg.items()}
     mano = MANO(**mano_cfg)
     if use_cuda:
@@ -195,4 +201,4 @@ def run_mano_twohands(init_trans, init_rot, init_hand_pose, is_right, init_betas
         "joints": torch.cat((outputs_left["joints"], outputs_right["joints"]), dim=0)
 
     }
-    return outputs_two
\ No newline at end of file
+    return outputs_two
diff --git a/infiller/hand_utils/process.py b/infiller/hand_utils/process.py
index bb40b94..73e5420 100644
--- a/infiller/hand_utils/process.py
+++ b/infiller/hand_utils/process.py
@@ -2,6 +2,32 @@ import torch
 from hand_utils.mano_wrapper import MANO
 from hand_utils.geometry_utils import aa_to_rotmat
 import numpy as np
+import os
+from pathlib import Path
+
+
+_HAWOR_ROOT = Path(__file__).resolve().parents[2]
+
+
+def _mano_config(left=False):
+    if left:
+        data_dir = _HAWOR_ROOT / "_DATA/data_left"
+        model_file = os.environ.get("MANO_LEFT")
+        model_dir = Path(model_file).parent if model_file else data_dir / "mano_left"
+    else:
+        data_dir = _HAWOR_ROOT / "_DATA/data"
+        model_file = os.environ.get("MANO_RIGHT")
+        model_dir = Path(model_file).parent if model_file else data_dir / "mano"
+    config = {
+        'DATA_DIR': str(data_dir),
+        'MODEL_PATH': str(model_dir),
+        'GENDER': 'neutral',
+        'NUM_HAND_JOINTS': 15,
+        'CREATE_BODY_POSE': False,
+    }
+    if left:
+        config['is_rhand'] = False
+    return config
 
 def run_mano(trans, root_orient, hand_pose, is_right=None, betas=None, use_cuda=True):
     """
@@ -13,13 +39,7 @@ def run_mano(trans, root_orient, hand_pose, is_right=None, betas=None, use_cuda=
     body_pose : B x T x J*3
     betas : (optional) B x D
     """
-    MANO_cfg = {
-        'DATA_DIR': '_DATA/data/',
-        'MODEL_PATH': '_DATA/data/mano',
-        'GENDER': 'neutral',
-        'NUM_HAND_JOINTS': 15,
-        'CREATE_BODY_POSE': False
-    }
+    MANO_cfg = _mano_config()
     mano_cfg = {k.lower(): v for k,v in MANO_cfg.items()}
     mano = MANO(**mano_cfg)
     if use_cuda:
@@ -89,14 +109,7 @@ def run_mano_left(trans, root_orient, hand_pose, is_right=None, betas=None, use_
     body_pose : B x T x J*3
     betas : (optional) B x D
     """
-    MANO_cfg = {
-        'DATA_DIR': '_DATA/data_left/',
-        'MODEL_PATH': '_DATA/data_left/mano_left',
-        'GENDER': 'neutral',
-        'NUM_HAND_JOINTS': 15,
-        'CREATE_BODY_POSE': False,
-        'is_rhand': False
-    }
+    MANO_cfg = _mano_config(left=True)
     mano_cfg = {k.lower(): v for k,v in MANO_cfg.items()}
     mano = MANO(**mano_cfg)
     if use_cuda:
@@ -168,4 +181,4 @@ def run_mano_twohands(init_trans, init_rot, init_hand_pose, is_right, init_betas
         "joints": torch.cat((outputs_left["joints"], outputs_right["joints"]), dim=0)
 
     }
-    return outputs_two
\ No newline at end of file
+    return outputs_two
diff --git a/lib/pipeline/masked_droid_slam.py b/lib/pipeline/masked_droid_slam.py
index 0c7c391..bdaf026 100644
--- a/lib/pipeline/masked_droid_slam.py
+++ b/lib/pipeline/masked_droid_slam.py
@@ -30,7 +30,10 @@ parser.add_argument("--calib", type=str, help="path to calibration file")
 parser.add_argument("--t0", default=0, type=int, help="starting frame")
 parser.add_argument("--stride", default=1, type=int, help="frame stride")
 
-parser.add_argument("--weights", default="weights/external/droid.pth")
+parser.add_argument(
+    "--weights",
+    default=os.environ.get("HAWOR_DROID_CKPT", "weights/external/droid.pth"),
+)
 parser.add_argument("--buffer", type=int, default=512)
 parser.add_argument("--image_size", default=[240, 320])
 parser.add_argument("--disable_vis", action="store_true")
@@ -285,4 +288,3 @@ def preprocess_masks(img_folder, masks):
 
 
 
-
diff --git a/lib/pipeline/tools.py b/lib/pipeline/tools.py
index 1869faf..071f2e6 100644
--- a/lib/pipeline/tools.py
+++ b/lib/pipeline/tools.py
@@ -1,3 +1,5 @@
+import os
+
 import cv2
 from tqdm import tqdm
 import numpy as np
@@ -19,8 +21,10 @@ else:
 
 
 def detect_track(imgfiles, thresh=0.5):
-    
-    hand_det_model = YOLO('./weights/external/detector.pt')
+    detector_checkpoint = os.environ.get(
+        "HAWOR_DETECTOR_CKPT", "./weights/external/detector.pt"
+    )
+    hand_det_model = YOLO(detector_checkpoint)
 
     # Run
     boxes_ = []
diff --git a/scripts/scripts_test_video/detect_track_video.py b/scripts/scripts_test_video/detect_track_video.py
index bce40b3..bc96f91 100644
--- a/scripts/scripts_test_video/detect_track_video.py
+++ b/scripts/scripts_test_video/detect_track_video.py
@@ -16,8 +16,9 @@ def extract_frames(video_path, output_folder):
 
     command = [
         'ffmpeg',
+        '-y',
         '-i', video_path,
-        '-vsync', '0',
+        '-fps_mode', 'passthrough',
         '-start_number', '0',
         os.path.join(output_folder, '%04d.png')
     ]
@@ -76,4 +77,4 @@ if __name__ == '__main__':
     parser.add_argument("--input_type", type=str, default='file')
     args = parser.parse_args()
 
-    detect_track_video(args)
\ No newline at end of file
+    detect_track_video(args)
diff --git a/scripts/scripts_test_video/hawor_slam.py b/scripts/scripts_test_video/hawor_slam.py
index c581d08..1dd98dd 100644
--- a/scripts/scripts_test_video/hawor_slam.py
+++ b/scripts/scripts_test_video/hawor_slam.py
@@ -90,7 +90,11 @@ def hawor_slam(args, start_idx, end_idx):
 
     # Estimate scale  
     block_print()  
-    metric = Metric3D('thirdparty/Metric3D/weights/metric_depth_vit_large_800k.pth') 
+    metric_checkpoint = os.environ.get(
+        "METRIC3D_CKPT",
+        "thirdparty/Metric3D/weights/metric_depth_vit_large_800k.pth",
+    )
+    metric = Metric3D(metric_checkpoint)
     enable_print() 
     min_threshold = 0.4
     max_threshold = 0.7
@@ -138,4 +142,3 @@ def hawor_slam(args, start_idx, end_idx):
 
 
 
-
diff --git a/thirdparty/DROID-SLAM/setup.py b/thirdparty/DROID-SLAM/setup.py
index fa45087..734db04 100644
--- a/thirdparty/DROID-SLAM/setup.py
+++ b/thirdparty/DROID-SLAM/setup.py
@@ -17,13 +17,7 @@ setup(
             ],
             extra_compile_args={
                 'cxx': ['-O3'],
-                'nvcc': ['-O3',
-                    # RTX 5090 (Blackwell, sm_120)
-                    '-gencode=arch=compute_120,code=sm_120',
-
-                    # PTX fallback (CRITICAL for future GPUs)
-                    '-gencode=arch=compute_120,code=compute_120',
-                ]
+                'nvcc': ['-O3'],
             }),
     ],
     cmdclass={ 'build_ext' : BuildExtension }
@@ -46,13 +40,7 @@ setup(
                 'thirdparty/lietorch/lietorch/src/lietorch_cpu.cpp'],
             extra_compile_args={
                 'cxx': ['-O2'],
-                'nvcc': ['-O2',
-                    # RTX 5090 (Blackwell, sm_120)
-                    '-gencode=arch=compute_120,code=sm_120',
-
-                    # PTX fallback (CRITICAL for future GPUs)
-                    '-gencode=arch=compute_120,code=compute_120',
-                ]
+                'nvcc': ['-O2'],
             }),
     ],
     cmdclass={ 'build_ext' : BuildExtension }

