diff --git a/generate_mesh_sam3d.py b/generate_mesh_sam3d.py
index 5b5f632..2edf260 100644
--- a/generate_mesh_sam3d.py
+++ b/generate_mesh_sam3d.py
@@ -43,6 +43,7 @@ python generate_mesh_sam3d.py --json_path /path/to/manifest.json --with_mesh_pos
 
 import argparse
 import os
+import sys
 import json
 from typing import Iterable, List, Optional, Sequence
 
@@ -54,7 +55,11 @@ from scipy.spatial.transform import Rotation as R
 from PIL import Image
 from loguru import logger
 
-from notebook.inference import (  # type: ignore[import-not-found]
+# The repository's `notebook/` directory is not a Python package and otherwise
+# loses name resolution to Jupyter's installed `notebook` package.
+_LOCAL_NOTEBOOK_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "notebook")
+sys.path.insert(0, _LOCAL_NOTEBOOK_DIR)
+from inference import (  # type: ignore[import-not-found]
     Inference,
     load_image,
     load_mask,
@@ -926,4 +931,4 @@ def main() -> None:
     )
 
 if __name__ == "__main__":
-    main()
\ No newline at end of file
+    main()
diff --git a/sam3d_objects/model/backbone/dit/embedder/dino.py b/sam3d_objects/model/backbone/dit/embedder/dino.py
index c489fe7..1674fae 100644
--- a/sam3d_objects/model/backbone/dit/embedder/dino.py
+++ b/sam3d_objects/model/backbone/dit/embedder/dino.py
@@ -1,4 +1,5 @@
 # Copyright (c) Meta Platforms, Inc. and affiliates.
+import os
 import torch
 from typing import Optional, Dict, Any
 import warnings
@@ -25,6 +26,18 @@ class Dino(torch.nn.Module):
         if backbone_kwargs is None:
             backbone_kwargs = {}
 
+        local_repo = os.environ.get("DINOV2_REPO")
+        if local_repo:
+            if not os.path.isfile(os.path.join(local_repo, "hubconf.py")):
+                raise FileNotFoundError(
+                    f"DINOV2_REPO does not contain hubconf.py: {local_repo}"
+                )
+            repo_or_dir = local_repo
+            source = "local"
+            # SAM3D loads the final DINO parameters from its own checkpoint.
+            # Avoid a redundant torch.hub download of upstream pretrained weights.
+            backbone_kwargs.setdefault("pretrained", False)
+
         with warnings.catch_warnings():
             warnings.simplefilter("ignore")
             
diff --git a/sam3d_objects/pipeline/inference_pipeline.py b/sam3d_objects/pipeline/inference_pipeline.py
index d34d5c2..1ee3f48 100644
--- a/sam3d_objects/pipeline/inference_pipeline.py
+++ b/sam3d_objects/pipeline/inference_pipeline.py
@@ -16,9 +16,10 @@ def set_attention_backend():
 
     logger.info(f"GPU name is {gpu_name}")
     if "A100" in gpu_name or "H100" in gpu_name or "H200" in gpu_name:
-        # logger.info("Use flash_attn")
-        os.environ["ATTN_BACKEND"] = "flash_attn"
-        os.environ["SPARSE_ATTN_BACKEND"] = "flash_attn"
+        # Use PyTorch's built-in Flash SDPA instead of requiring a separate
+        # flash-attn extension build for the default inference setup.
+        os.environ["ATTN_BACKEND"] = "torch_flash_attn"
+        os.environ["SPARSE_ATTN_BACKEND"] = "torch_flash_attn"
 
 set_attention_backend()
 

