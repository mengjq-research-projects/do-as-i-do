[project]
name = "do-as-i-do-workspace"
version = "0.1.0"
description = "Workspace-level uv environment for the Do as I Do repository"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = [
  "ruff>=0.13.2",
]

[tool.uv]
package = false